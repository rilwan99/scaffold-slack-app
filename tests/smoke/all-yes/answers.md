# Scenario: all-yes

Interview answers:
- Q1: name=`Acme Bot`, description=`Posts daily standup reminders`, bot_user_handle=`acmebot`
- Q2: `internal`
- Q3 (DMs): yes
- Q4 (mentions): yes
- Q5 (slash commands): `/standup, /skip`

Expected manifest in `expected/manifest.json`. Diff against the skill's actual output to verify.
